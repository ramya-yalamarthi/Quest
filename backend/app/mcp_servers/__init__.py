"""MCP servers (Phase 1) — expose the existing orchestrator capabilities as
standardized, discoverable MCP tools. Additive: wraps the same code the live
pipeline uses; does not change the live pipeline."""
